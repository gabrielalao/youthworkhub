<script type="text/javascript">
  <% if current_user %>
    window.currentUser = <%= render(
      "api/users/user.json.jbuilder", user: current_user
    ).html_safe %>
  <% elsif current_worker %>
    window.currentUser = <%= render(
      "api/workers/worker.json.jbuilder", worker: current_worker
    ).html_safe %>
  <% end %>
</script>

<main id="root">
</main>
